<h1>Editing Space</h1>

 <% if @book.errors.any? %>
  <%= @book.errors.count %>error(s) prohibited this book form being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<%= form_with model: @book do |f| %>

  <h4>Title</h4>
  <%= f.text_field :book_name %>

  <h4>Body</h4>
  <%= f.text_area :book_body %>

  <%= f.submit 'Update Book' %>

<% end %>

<table>
<tr>

<td><%= link_to "show", book_path(@book.id) %></td>

<td><p>|</p></td>

<td><p><a href ="/books">back</a></p></td>
</tr>
</table>